<Window x:Class="Proiect.SearchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Search" Height="200" Width="400">
	<Grid Margin="10">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<TextBlock Grid.Row="0" Text="Start Date:" VerticalAlignment="Center"/>
		<DatePicker x:Name="StartDatePicker" Grid.Row="0" Margin="120,0,0,0" VerticalAlignment="Center"/>

		<TextBlock Grid.Row="1" Text="End Date:" VerticalAlignment="Center" Margin="0,10,0,0"/>
		<DatePicker x:Name="EndDatePicker" Grid.Row="1" Margin="120,10,0,0" VerticalAlignment="Center"/>

		<Button x:Name="SearchButton" Grid.Row="2" Content="Search" Margin="0,10,0,0" HorizontalAlignment="Right" Width="100" Click="SearchButton_Click"/>
	</Grid>
</Window>